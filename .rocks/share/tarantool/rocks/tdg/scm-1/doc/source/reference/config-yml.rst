YAML configuration
==================

This reference explains the structure and parameters of ``config.yml`` which is the main configuration file of the system.
It defines the logic of processing the incoming requests and contains the settings of cluster roles and other system functions.
